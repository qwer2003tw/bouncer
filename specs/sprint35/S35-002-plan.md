# S35-002: Fix Pre-existing Test Failures — TestOrphanApprovalCleanup - Plan

## Phase 1: 診斷當前測試失敗

### 1.1 執行失敗的測試

```bash
pytest tests/test_callbacks_main.py::TestOrphanApprovalCleanup::test_execute_telegram_failure_ddb_no_record_returns_error -v
pytest tests/test_callbacks_main.py::TestOrphanApprovalCleanup::test_execute_telegram_exception_ddb_no_record_returns_error -v
```

**記錄輸出：**
- Assertion 失敗的具體訊息
- `content['status']` 的實際值
- DDB orphan records 是否存在

### 1.2 驗證 notifications.py 行為

建立臨時測試檔案 `test_notification_behavior.py`：

```python
from unittest.mock import patch, MagicMock
import sys
sys.path.insert(0, 'src')

def test_send_approval_request_with_empty_result():
    """測試 send_approval_request 在 Telegram API 回傳 {} 時的行為"""
    with patch('telegram.send_message_with_entities') as mock_tg:
        mock_tg.return_value = {}

        from notifications import send_approval_request

        result = send_approval_request(
            request_id='test-empty',
            command='aws s3 ls',
            reason='test empty result',
        )

        print(f"Empty result → ok={result.ok}, message_id={result.message_id}")
        assert result.ok == False, f"Expected ok=False, got {result.ok}"
        assert result.message_id is None

def test_send_approval_request_with_ok_false():
    """測試 send_approval_request 在 Telegram API 回傳 {'ok': False} 時的行為"""
    with patch('telegram.send_message_with_entities') as mock_tg:
        mock_tg.return_value = {'ok': False}

        from notifications import send_approval_request

        result = send_approval_request(
            request_id='test-ok-false',
            command='aws s3 ls',
            reason='test ok false',
        )

        print(f"ok=False result → ok={result.ok}, message_id={result.message_id}")
        assert result.ok == False
        assert result.message_id is None

def test_send_approval_request_with_exception():
    """測試 send_approval_request 在 Telegram API 拋出 exception 時的行為"""
    with patch('telegram.send_message_with_entities') as mock_tg:
        mock_tg.side_effect = Exception("Connection refused")

        from notifications import send_approval_request

        # 檢查是否會 propagate exception，或是 catch 並回傳 NotificationResult(ok=False)
        try:
            result = send_approval_request(
                request_id='test-exception',
                command='aws s3 ls',
                reason='test exception',
            )
            print(f"Exception caught → ok={result.ok}, message_id={result.message_id}")
            assert result.ok == False
        except Exception as e:
            print(f"Exception propagated: {e}")
            # 如果 exception propagate，測試應該 mock exception 並驗證 cleanup 邏輯

if __name__ == '__main__':
    test_send_approval_request_with_empty_result()
    test_send_approval_request_with_ok_false()
    test_send_approval_request_with_exception()
```

執行：
```bash
python test_notification_behavior.py
```

**預期結果：**
- 了解 `send_approval_request` 在各種失敗場景下的行為
- 確定 exception 是被 catch 還是 propagate

## Phase 2: 檢查 mcp_execute.py Cleanup 邏輯

### 2.1 搜尋 _submit_for_approval 函數

```bash
grep -n "_submit_for_approval" src/mcp_execute.py
```

### 2.2 檢查 cleanup 邏輯

在 `_submit_for_approval` 函數中搜尋：

```bash
grep -A 20 "notified = send_approval_request" src/mcp_execute.py | grep -E "if not notified|notified.ok"
```

**預期：**
- 如果有 `if not notified.ok:` 或 `if not notified:` → cleanup 邏輯已存在
- 如果沒有 → 需要新增 cleanup 邏輯

### 2.3 如果 cleanup 邏輯不存在，需要新增

**檔案：** `src/mcp_execute.py`

找到 `_submit_for_approval` 函數，在 `send_approval_request` 之後加入：

```python
def _submit_for_approval(ctx: ExecuteContext) -> dict:
    # ... existing code: create DDB record ...

    # Send Telegram notification
    notified = send_approval_request(
        request_id=ctx.req_id,
        command=ctx.command,
        reason=ctx.reason,
        timeout=ctx.timeout,
        source=ctx.source,
        account_id=ctx.account_id,
        account_name=ctx.account_name,
        assume_role=ctx.assume_role,
        context=ctx.context,
        template_scan_result=ctx.template_scan_result,
    )

    # Check if notification succeeded
    if not notified.ok:
        # Cleanup: delete DDB record to avoid orphan
        try:
            table.delete_item(Key={'request_id': ctx.req_id})
            logger.warning(
                "Telegram notification failed, DDB record cleaned up",
                extra={
                    "src_module": "execute",
                    "operation": "submit_approval",
                    "request_id": ctx.req_id,
                }
            )
        except ClientError as cleanup_exc:
            logger.error(
                "Failed to cleanup DDB record after Telegram failure: %s",
                cleanup_exc,
                extra={
                    "src_module": "execute",
                    "operation": "submit_approval_cleanup",
                    "request_id": ctx.req_id,
                    "error": str(cleanup_exc),
                }
            )

        return mcp_error(
            ctx.req_id,
            "Failed to send approval notification. Please try again.",
        )

    # Success path: schedule expiry, etc.
    post_notification_setup(ctx.req_id, notified.message_id, expires_at)
    # ... rest of existing code ...
```

## Phase 3: 修改測試 Mock

### 3.1 修改 test_execute_telegram_failure_ddb_no_record_returns_error

**檔案：** `tests/test_callbacks_main.py` (Line 929)

**變更：**

```python
@patch('telegram.send_message_with_entities')
def test_execute_telegram_failure_ddb_no_record_returns_error(self, mock_telegram, mock_dynamodb, app_module):
    """Telegram 失敗（empty response）→ DDB 無 record，回 error ✅"""
    # 根據 Phase 1 的診斷結果，選擇正確的 mock
    # 選項 A: empty dict（如果 notifications.py 會將 {} 視為失敗）
    mock_telegram.return_value = {}

    # 選項 B: {'ok': False}（如果 notifications.py 檢查 result.get('ok')）
    # mock_telegram.return_value = {'ok': False}

    # ... rest of test remains the same ...
```

### 3.2 修改 test_execute_telegram_exception_ddb_no_record_returns_error

**檔案：** `tests/test_callbacks_main.py` (Line 972)

**變更：**

根據 Phase 1 的診斷結果：
- **如果 `send_approval_request` catch exception 並回傳 `NotificationResult(ok=False)`：**
  ```python
  # Mock 回傳 failure 而非 exception
  mock_telegram.return_value = {}  # or {'ok': False}
  ```

- **如果 `send_approval_request` 不 catch exception（propagate）：**
  ```python
  # Mock exception（當前測試已正確）
  mock_telegram.side_effect = Exception("Telegram connection refused")
  ```

## Phase 4: 執行測試驗證

### 4.1 執行修改後的測試

```bash
pytest tests/test_callbacks_main.py::TestOrphanApprovalCleanup -v
```

**預期輸出：**
```
test_execute_telegram_success_ddb_has_record_returns_pending PASSED
test_execute_telegram_failure_ddb_no_record_returns_error PASSED
test_execute_telegram_exception_ddb_no_record_returns_error PASSED
test_upload_telegram_failure_ddb_no_record_returns_error PASSED
test_upload_telegram_success_ddb_has_record_returns_pending PASSED
```

### 4.2 執行完整測試套件

```bash
pytest tests/ -v
```

確保無 regression。

### 4.3 檢查 coverage

```bash
pytest tests/test_callbacks_main.py --cov=src --cov-report=term-missing
```

確保 coverage 不下降。

## Phase 5: Code Review & Merge

### 5.1 建立 PR

```bash
git add tests/test_callbacks_main.py

# 如果修改了 mcp_execute.py
git add src/mcp_execute.py

git commit -m "fix(tests): S35-002 - fix TestOrphanApprovalCleanup failures"
git push origin s35-002-fix-orphan-approval-tests
```

### 5.2 PR Description

```markdown
## S35-002: Fix TestOrphanApprovalCleanup Test Failures

### 問題
`TestOrphanApprovalCleanup` 的兩個測試失敗：
1. `test_execute_telegram_failure_ddb_no_record_returns_error`
2. `test_execute_telegram_exception_ddb_no_record_returns_error`

### 根本原因
- Mock 的 `telegram.send_message_with_entities` 回傳值不正確
- [如果有] `mcp_execute.py` 缺少 cleanup 邏輯

### 修復方法
- 修正測試 mock（回傳 `{}` 或 `{'ok': False}`）
- [如果有] 在 `_submit_for_approval` 加入 DDB cleanup 邏輯

### 測試
- [x] `TestOrphanApprovalCleanup` 全部通過
- [x] 無 regression（其他測試仍通過）
- [x] Coverage 不下降
```

### 5.3 PR Checklist

- [ ] 測試通過
- [ ] 無 regression
- [ ] Code coverage 不下降
- [ ] [如果修改了 mcp_execute.py] cleanup 邏輯已加入 logger
- [ ] [如果修改了 mcp_execute.py] cleanup 邏輯有 exception handling

## Phase 6: Post-Merge 驗證

### 6.1 在 CI 執行完整測試

確保 CI pipeline 全部通過。

### 6.2 無需部署

此修復只影響測試，不影響 production code（除非新增了 cleanup 邏輯）。

如果新增了 cleanup 邏輯，在下次 `sam deploy` 時會一起部署。

## 回滾計畫

如果修改導致其他測試失敗：

```bash
# 回滾測試檔案
git checkout HEAD~1 tests/test_callbacks_main.py

# 或回滾 mcp_execute.py（如果修改了）
git checkout HEAD~1 src/mcp_execute.py
```

## 注意事項

1. **Cleanup 邏輯的副作用：** 如果新增 cleanup 邏輯，確保只在 Telegram 失敗時觸發，不影響正常流程
2. **Exception handling：** cleanup 失敗不應該影響 error 回傳（已 log warning）
3. **向後相容：** cleanup 邏輯不影響已存在的 approval requests

## 診斷結果範本

填寫以下表格（執行 Phase 1 後）：

| 場景 | send_approval_request 行為 | 是否需要修改測試 | 是否需要新增 cleanup |
|------|---------------------------|----------------|-------------------|
| Telegram 回傳 `{}` | `NotificationResult(ok=False, ...)` | ✅ Mock 正確 / ❌ 需修改 | ✅ 已有 / ❌ 需新增 |
| Telegram 回傳 `{'ok': False}` | `NotificationResult(ok=False, ...)` | ✅ / ❌ | ✅ / ❌ |
| Telegram exception | propagate / catch → `NotificationResult(ok=False, ...)` | ✅ / ❌ | ✅ / ❌ |

## 成功標準

- [ ] `test_execute_telegram_failure_ddb_no_record_returns_error` 通過
- [ ] `test_execute_telegram_exception_ddb_no_record_returns_error` 通過
- [ ] 完整測試套件通過
- [ ] Coverage 不下降
- [ ] PR merged
