# s37-004 Plan — fix: auto_approved 通知補帳號欄位

## 改動範圍

| 檔案 | 函數 | 改動類型 |
|------|------|---------|
| `src/mcp_execute.py` | `_check_auto_approve()` | 在 `_notif_text` 加帳號行 |
| `tests/test_mcp_execute.py` | 新增 regression test | 驗證帳號欄位出現在通知中 |

## 實作細節

### `src/mcp_execute.py`

找到 `_notif_text` 的建構位置（約 line 629-640），在 `reason_line` 之前加入：

```python
account_line = (
    f"🏦 *帳號：* `{ctx.account_id}` ({escape_markdown(ctx.account_name or '')})\n"
    if ctx.account_id else ""
)
```

然後把 `account_line` 插入 `_notif_text` format string 中，位置在來源行之後、reason 行之前。

## 測試策略

### 新增 regression test（`tests/test_mcp_execute.py`）

Mock path: `src/mcp_execute.py` 中的 `send_telegram_message`（或 entities sender）

```python
def test_auto_approved_notification_includes_account():
    # Given: safelist command, account 992382394211 (Dev)
    # When: _check_auto_approve() runs
    # Then: notification text contains '992382394211' and 'Dev'
```

測試矩陣：
1. 有帳號 ID → 帳號行出現
2. 無帳號 ID（空字串）→ 帳號行不出現
3. 帳號名稱含特殊字元 → escape 正確

## TCS 評分

| 維度 | 分數 | 說明 |
|------|------|------|
| D1 Files | 1 | 只改 1 個 src 檔 |
| D2 Cross-module | 0 | 無跨模組改動 |
| D3 Testing | 2 | 補測試 |
| D4 Infrastructure | 0 | 無 template.yaml 改動 |
| D5 External | 0 | 無新 AWS API |
| **TCS** | **3** | **Simple** |
