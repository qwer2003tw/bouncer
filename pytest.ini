# Bouncer 測試配置
# 注意：不在 pytest.ini 開啟 -n xdist，避免無限制情況下 OOM
# 全套測試請用：bash scripts/run-tests.sh --all
[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
filterwarnings = ignore::DeprecationWarning
